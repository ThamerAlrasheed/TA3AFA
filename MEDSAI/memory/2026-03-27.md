## OpenClaw local setup

- User has a working local OpenClaw setup outside this repo under `/Users/mac/openclaw`.
- Live Docker container is `openclaw`, image `ghcr.io/openclaw/openclaw:latest`, published only on `127.0.0.1:18789` and `127.0.0.1:18791`, with restart policy `unless-stopped`.
- OpenClaw runtime is bind-mounted from `/Users/mac/openclaw/runtime` and live config is `/Users/mac/openclaw/runtime/openclaw.json`.
- OpenClaw is configured to use host Ollama at `http://host.docker.internal:11434`.
- Active model is `ollama/qwen2.5-coder:7b`; installed Ollama models also include `glm-4.7-flash:latest` and `llama3:latest`.
- Host OpenClaw config at `/Users/mac/.openclaw/openclaw.json` was rewritten to match the Docker/Ollama setup and validates successfully; old host config was backed up to `/Users/mac/openclaw/backups/host-openclaw.json.before-docker-sync`.
- README for the setup lives at `/Users/mac/openclaw/README.md`.
- Browser device pairing for the OpenClaw Control UI was approved on 2026-03-27; pending requests were cleared afterward.
- Ollama itself is healthy and fast for direct calls (roughly 2.3s for a tiny test), but OpenClaw chat is much slower because the default local `coding` profile sends a large agent/system prompt. A direct OpenClaw agent smoke test on `qwen2.5-coder:7b` succeeded but took about 48.7s with roughly 9.1k input tokens.
- User was told the recommended next optimization is to make OpenClaw chat-first instead of coding-first if they want faster replies.
