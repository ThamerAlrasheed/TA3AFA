You are working on my iOS SwiftUI app called ISTSEH / استصح.

The current Settings page looks messy, inefficient, and unprofessional. I want you to redesign it so it feels like a clean, modern, native iOS healthcare app settings screen.

Do not only make visual changes. Also reorganize the settings so they match the purpose of the app: medication care, patient/caregiver management, medical profile, routine, reminders, privacy, and support.

====================================================
GOAL
====================================================

Redesign the Settings experience to be:

1. Professional iOS-style
2. Clean and not crowded
3. More useful for a medication-care app
4. Easy for patients and caregivers to understand
5. Consistent with Apple Settings / Health app style
6. Not overly custom or messy
7. Safe with patient/caregiver context
8. Better organized into logical sections

Current problems:
- Too much content is crammed into one long page.
- The page visually feels unfinished.
- The top title/header spacing is inconsistent.
- Routine settings are mixed directly into the main Settings page.
- Notification settings are mixed into the main Settings page.
- The font size feature is unnecessary.
- Some rows look too large while others feel random.
- The bottom tab bar overlaps or visually fights with content.
- It does not feel like a polished iOS app.

====================================================
IMPORTANT REQUIREMENT
====================================================

Remove the font size feature completely.

Remove:
- Small / Medium / Large segmented font size control
- Any related AppSettings value if only used for this feature
- Any UI code related to font size selection
- Any unused state/model code caused by removing it

Do not remove language selection.

====================================================
FILES TO REVIEW
====================================================

Inspect the real repo first. Likely files:

- MEDSAI/Settings page/SettingsView.swift
- MEDSAI/Settings page/PatientSettingsView.swift
- MEDSAI/Settings page/AppSettings.swift
- MEDSAI/Settings page/MedicalProfileView.swift
- MEDSAI/App/RootTabView.swift
- MEDSAI/Services/PatientSessionStore.swift
- MEDSAI/Utilities/NotificationsManager.swift

Search for:
- font size
- text size
- appearance
- routine
- reminders
- notification settings
- medical profile
- caregiver
- activePatientID
- activePatientName

====================================================
NEW SETTINGS STRUCTURE
====================================================

Redesign the Settings page into a cleaner structure.

Use a modern iOS grouped layout. Prefer:
- NavigationStack
- ScrollView or List with grouped sections
- Rounded cards
- Native-looking rows
- SF Symbols
- Proper spacing
- Clear section headers
- Subtitles where helpful
- Chevron indicators for navigation rows
- Toggle rows only where settings can be changed immediately

The main Settings page should be a hub, not a dumping ground.

Suggested structure:

Header:
- Small profile/status card at the top
- Show current mode:
  - Patient mode: “Patient profile”
  - Caregiver mode with selected patient: “Managing [Patient Name]”
  - Caregiver mode without selected patient: “No patient selected”
- Optional subtitle:
  - “Manage care settings, medical profile, reminders, and app preferences.”

Section 1: Care
Rows:
1. Family Members
   - Icon: person.2.fill
   - Subtitle: “Manage patients and care access”
   - Opens existing family member management

2. Medical Profile
   - Icon: cross.case.fill or heart.text.square.fill
   - Subtitle: “Allergies and chronic conditions”
   - Opens MedicalProfileView
   - In caregiver mode, must manage selected patient

3. Daily Routine
   - Icon: clock.fill or sun.max.fill
   - Subtitle: “Wake time, meals, bedtime”
   - Opens a separate Routine Settings screen
   - Do NOT show all routine fields directly on main Settings page

4. Reminders
   - Icon: bell.badge.fill
   - Subtitle: “Medication and appointment reminders”
   - Opens a separate Reminder Settings screen or integrates inside Routine screen if cleaner

Section 2: App Preferences
Rows:
1. Appearance
   - Use Light / Dark / System segmented picker
   - Keep it neat inside a row/card
   - Do NOT include font size picker

2. Language
   - Keep English picker
   - If Arabic support exists, keep Arabic option too
   - Show as a row with current language on trailing side

Section 3: Help & Legal
Rows:
- Tutorial
- FAQ
- Privacy Policy
- Terms of Service
- Contact Support

Section 4: Account / Session
Only if these actions already exist:
- Sign out
- Disconnect patient mode
- Clear selected patient
- Manage session

Do not invent destructive buttons unless supported.

====================================================
VISUAL DESIGN REQUIREMENTS
====================================================

Make it look like a native polished iOS app.

Design style:
- Background: iOS grouped background color
- Cards: white or systemBackground with rounded corners
- Section spacing: generous but not huge
- Row height: consistent, around 56 to 68 points
- Icons: SF Symbols in small colored rounded squares
- Typography:
  - Large title “Settings” at top
  - Section titles in secondary text
  - Row title: semibold/body
  - Row subtitle: caption/subheadline gray
- Avoid huge empty gaps.
- Avoid random floating controls.
- Avoid oversized pills everywhere.
- Avoid placing routine time pickers directly on the main settings page.
- Avoid tab bar overlap by using safeAreaInset/padding.

The page should feel close to:
- Apple Settings
- Apple Health
- iOS Reminders settings
- A clean healthcare app

====================================================
ROUTINE SETTINGS SCREEN
====================================================

Create or improve a separate Routine Settings screen.

Screen title:
- “Daily Routine”

Description:
- “These times help ISTSEH schedule medication reminders around your day.”

Fields:
- Wake time
- Bedtime
- Breakfast
- Lunch
- Dinner
- Timezone if already supported

Use DatePicker with displayedComponents: .hourAndMinute.

Patient mode:
- Load and save patient’s own routine.

Caregiver mode:
- Load and save selected patient’s routine.
- Show “Managing [Name]’s routine” at top.

Important:
- Do not use caregiver’s own routine for selected patient meds.
- Routine must be tied to activePatientID in caregiver mode.
- Routine must use device_token in patient mode.

Save button:
- Top-right “Save” or bottom primary button.
- Disabled if no changes.
- Show loading state.
- On success show:
  “Routine updated. Medication reminders were refreshed.”

After save:
- Update local AppSettings/shared routine state.
- Refresh medication reminders.
- Recalculate auto schedules only where is_manual_schedule == false.
- Do not overwrite manual schedules.

====================================================
REMINDER SETTINGS SCREEN
====================================================

Create or improve a separate Reminders screen if cleaner than keeping toggles inside Routine.

Screen title:
- “Reminders”

Fields:
- Medication reminders enabled
- Appointment reminders enabled

Optional explanatory text:
- “Medication reminders are scheduled using this patient’s medication plan and daily routine.”

Patient mode:
- Save own reminder settings.

Caregiver mode:
- Save selected patient’s reminder settings.

After save:
- Refresh notifications.

If the current backend stores these as part of routine, keep them in the Routine Settings screen instead. Do not duplicate storage.

====================================================
MEDICAL PROFILE ROW BEHAVIOR
====================================================

The Medical Profile row must open MedicalProfileView.

Patient mode:
- Edits own allergies and conditions.

Caregiver mode:
- Edits selected patient’s allergies and conditions.
- Show context clearly:
  “Managing [Patient Name]’s profile”

If caregiver mode has no selected patient:
- Disable rows that require selected patient OR show a clear message asking user to select a patient first.
- Do not accidentally edit caregiver’s own profile.

====================================================
FAMILY MEMBERS ROW
====================================================

Keep the existing family management flow.

Make the row look polished:
- Title: “Family Members”
- Subtitle: “Manage patients and care access”
- Icon: person.2.fill
- Chevron

Do not break caregiver dropdown or active patient selection.

====================================================
APP PREFERENCES
====================================================

Appearance:
- Keep Light / Dark / System.
- Make it visually clean.
- It can be a segmented picker inside an Appearance screen or inline card.
- Use native SwiftUI Picker style.

Language:
- Keep language selection.
- Make it a clean row.
- Do not make the UI cluttered.

Remove font size:
- Completely remove from Settings UI.
- Remove related unused code if safe.

====================================================
PATIENT CONTEXT REQUIREMENTS
====================================================

Do not break existing active patient behavior.

Source of truth:
- Use existing PatientSessionStore / AppSettings / environment object pattern.
- Do not create a second unrelated active patient state.

Every patient-specific settings screen must resolve context correctly:

Patient mode:
- use device_token
- no selected patient required

Caregiver mode:
- use activePatientID from caregiver dropdown
- send Authorization header
- do not send stale device_token
- display activePatientName when available

Screens affected:
- MedicalProfileView
- Routine Settings
- Reminder Settings if separate
- Add Medication smart scheduling must still use selected patient routine

====================================================
BACKEND / DATA REQUIREMENTS
====================================================

If routine settings are currently local-only, move them to remote storage or connect to existing remote function.

Preferred:
- Extend patient-profile Edge Function with:
  - get_routine
  - update_routine

Or use existing patient-routine Edge Function if already present.

Do not invent a new backend path if one already exists.

Auth:
- Patient mode: device_token
- Caregiver mode: Authorization + patient_id
- Service role only after verifying patient context

Audit:
- routine_updated when routine changes

Do not fake backend success.

====================================================
IMPLEMENTATION QUALITY
====================================================

Use clean reusable components if helpful:

Possible components:
- SettingsHeaderCard
- SettingsSectionCard
- SettingsNavigationRow
- SettingsToggleRow
- SettingsIconBadge
- RoutineSettingsView
- ReminderSettingsView

Keep components in the same file if small, or split cleanly if the repo style supports it.

Avoid massive messy views.

Use system colors:
- Color(.systemGroupedBackground)
- Color(.secondarySystemGroupedBackground)
- Color(.systemBackground)
- .secondary
- .primary

Use SF Symbols only.

Make sure the design works with:
- Light mode
- Dark mode
- iPhone 16e / similar simulator
- Dynamic safe area / tab bar

====================================================
TESTING REQUIRED
====================================================

After implementing, run:

1. Build:
Use xcodebuild with the correct project/workspace and scheme.

First inspect:
xcodebuild -list

Then build with the actual scheme, for example:
xcodebuild \
  -project MEDSAI.xcodeproj \
  -scheme MEDSAI \
  -destination 'platform=iOS Simulator,name=iPhone 16e' \
  build

If iPhone 16e destination is unavailable, use an available iPhone simulator.

2. Manual UI checks:
- Settings page loads.
- No font size control exists.
- Main page is clean and organized.
- Medical Profile opens.
- Family Members opens.
- Routine Settings opens.
- Reminder settings work if separate.
- Appearance control still works.
- Language row still works.
- Bottom tab bar does not overlap content.
- Caregiver selected patient name appears where relevant.
- Patient-specific screens do not edit the wrong patient.

3. Context checks:
- Patient mode routine edits own routine.
- Caregiver mode routine edits selected patient routine.
- Switching patients loads the selected patient’s routine.
- Add Medication smart scheduling uses selected patient routine.

====================================================
FINAL REPORT FORMAT
====================================================

Return a report with this exact structure:

1. Settings redesign summary
- What changed visually
- What changed structurally
- What was removed

2. New Settings structure
- Header
- Care section
- App Preferences section
- Help & Legal section
- Account/session section if present

3. Font size removal
- Files/values removed
- Confirm no font size UI remains

4. Routine Settings
- Screen behavior
- Patient mode behavior
- Caregiver mode behavior
- Storage/backend behavior
- Notification refresh behavior
- Manual schedule protection

5. Reminder Settings
- Where it lives
- What toggles exist
- How it saves

6. Patient context safety
- Source of activePatientID / activePatientName
- How Medical Profile uses it
- How Routine uses it
- How caregiver mode avoids stale device_token

7. Files changed
- List every changed file

8. Tests run
- Build result
- Manual UI checks
- Patient/caregiver context checks

9. Remaining TODOs
- Only include real TODOs that still remain